% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_forecast.R
\name{plot_forecast}
\alias{plot_forecast}
\title{Plot Forecasts with Actual Sales for Selected SKUs}
\usage{
plot_forecast(full_data, forecast_df_ci, sku_ids = "all", aggregate_all = TRUE)
}
\arguments{
\item{full_data}{A data.frame containing the actual sales data with at least
the columns: `week`, `sku`, and `weekly_sales`.}

\item{forecast_df_ci}{A data.frame containing forecasted values and
confidence intervals. Required columns: `week`, `sku`, `forecast`,
`lower`, `upper`.}

\item{sku_ids}{A character vector or numeric vector of SKUs to include in 
the plot. Use `"all"` (default) to select all SKUs.}

\item{aggregate_all}{Logical. If `TRUE` and multiple SKUs are selected, 
their values will be aggregated into a single combined plot. Defaults to `TRUE`.}
}
\value{
An interactive plotly subplot object showing actuals vs forecasts 
  with uncertainty bands, arranged by SKU.
}
\description{
This function creates interactive line plots using `ggplot2` and `plotly` to 
compare forecasted sales (with confidence intervals) against actual sales 
for one or more SKUs. Users can optionally aggregate all SKUs and display 
a combined plot.
}
\examples{
\dontrun{
plot_forecast_with_actuals(
  full_data = actual_data,
  forecast_df_ci = forecast_data,
  sku_ids = c(101, 102),
  aggregate_all = FALSE
)
}

}

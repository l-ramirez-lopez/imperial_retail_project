% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_ranges_for_skus.R
\name{calculate_ranges_for_skus}
\alias{calculate_ranges_for_skus}
\title{Calculate Revenue, Price, and Sales Ranges for Selected SKUs}
\usage{
calculate_ranges_for_skus(
  mdata,
  selected_rows,
  my_price_change,
  my_dropdown_elast,
  my_variable,
  my_lambdas,
  my_variables,
  sku_price_change_lims,
  melasticity,
  get_sales_revenue_change
)
}
\arguments{
\item{mdata}{A data frame containing SKU information (e.g., one with columns \code{SKU}, \code{price}, etc.).}

\item{selected_rows}{A vector of row indices (e.g., \code{input$my_elasticity_rows_selected}) identifying which SKUs are chosen.}

\item{my_price_change}{Numeric. The user's chosen percentage price change (e.g., \code{input$my_price_change}).}

\item{my_dropdown_elast}{A character string indicating which lambda value was selected (e.g., \code{input$my_dropdown_elast}).}

\item{my_variable}{A character string indicating another selected variable (e.g., \code{input$my_variable}).}

\item{my_lambdas}{A data frame or list that maps user-facing lambda values to internal aliases (e.g., with columns \code{lambda_user} and \code{alias}).}

\item{my_variables}{A data frame or list that maps user-facing variable names to internal aliases (e.g., columns \code{variables_user} and \code{alias}).}

\item{sku_price_change_lims}{A list containing min/max data for each SKU-lambda combination (e.g., \code{sku_price_change_lims[["sku_1"]][["my_lambda_alias"]]}).}

\item{melasticity}{An object or data needed by \code{get_sales_revenue_change} for elasticity calculations.}

\item{get_sales_revenue_change}{A function of the form \code{function(sku, price_change_pct, melasticity, mdata) \{\}} that returns a list with a \code{results} data frame including columns \code{new_revenue}, \code{new_sales_qty}, \code{new_price}, etc.}
}
\value{
A named list with three data frames: \code{revenue}, \code{price}, and \code{sales}. Each includes columns \code{sku}, \code{date}, \code{x}, \code{min}, and \code{max}.
}
\description{
Calculate Revenue, Price, and Sales Ranges for Selected SKUs
}
\examples{
\dontrun{
result_list <- calculate_ranges_for_skus(
  mdata = my_data,
  selected_rows = input$my_elasticity_rows_selected,
  my_price_change = input$my_price_change,
  my_dropdown_elast = input$my_dropdown_elast,
  my_variable = input$my_variable,
  my_lambdas = my_lambdas,
  my_variables = my_variables,
  sku_price_change_lims = sku_price_change_lims,
  melasticity = melasticity,
  get_sales_revenue_change = get_sales_revenue_change
)

head(result_list$revenue)
}
}
